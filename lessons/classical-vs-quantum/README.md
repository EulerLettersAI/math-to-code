# Classical and quantum kernel classifiers

Open `index.ipynb` in JupyterLab to run the lesson, or read the rendered
version on the Math to Code website.

The quantum classifier uses an exact statevector simulation. It does not
require an IBM Quantum account or access to quantum hardware.

