# SVM: Classical and quantum kernel classifiers

Open the individual `.ipynb` notebooks in JupyterLab to run the lesson, or read
the series overview in `index.qmd` and the rendered Math to Code website.

The quantum classifier uses an exact statevector simulation. It does not
require an IBM Quantum account or access to quantum hardware.
